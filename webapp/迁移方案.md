# bookmadebook 自助听书 · 平台迁移方案（Cloudflare → 阿里云 FC）

> 状态：方案定稿 + 代码迁移完成（2026-09-05，master c269937 之后的工作区改动）。
> 真实部署由 008 负责（本方案含「008 控制台操作清单」），本仓库只交付代码 + 方案。
> 结论先行：**订单数据库选「表格存储 Tablestore（按量）」，函数与表同地域建在 `cn-hongkong`**。

---

## 1. 架构图（迁移后）

```
┌─────────────────────────── 用户（小红书 H5 进入） ───────────────────────────┐
│                                                                             │
│  浏览器 ──HTTPS──▶ 自定义域名（book.xxx.com，香港地域免备案）                  │
│                        │  CNAME → FC 提供域名                                 │
│                        ▼                                                    │
│              ┌─────────────────────────────────────┐                        │
│              │  函数计算 FC 3.0（Node.js 20）        │                        │
│              │  codeUri: ./fc/（同源托管，免跨域）    │                        │
│              │  ├─ /api/*  → worker.js route()      │                        │
│              │  └─ 其它    → public/ 静态 H5 文件     │                        │
│              └──────────────┬──────────────────────┘                        │
│                             │ 订单读写（同地域内网/公网 endpoint）            │
│                             ▼                                              │
│              ┌──────────────────────────────┐                              │
│              │ Tablestore 表格存储（按量计费） │                              │
│              │  orders 表：PK=id，一行一单      │                              │
│              │  rate_limits 表：PK=email#day  │                              │
│              └──────────────┬───────────────┘                              │
└─────────────────────────────┼──────────────────────────────────────────────┘
                              │ 30s 轮询 paid 订单（公网 endpoint，量小费用可忽略）
                              ▼
              ┌──────────────────────────────┐
              │ 本地电脑 daemon（无需改代码）    │
              │  A 写稿 → B 生成 mp3 → PATCH    │
              │  产物：本地 webapp-output/      │
              └──────────────┬───────────────┘
                             ▼（下一轮任务）
                     OSS 对象存储（mp3 下载链接）
```

Cloudflare 原路径保留可用（`wrangler dev` / D1 测试照旧），迁移是**加一层平台适配**，不是重写。

---

## 2. 数据库选型结论

| 方案 | 成本 | 公网直连（daemon） | 结论 |
|---|---|---|---|
| **Tablestore 表格存储（按量）** | 无实例月费，按读写 CU 计费（量小≈几元/月量级；免费额度以控制台为准） | ✅ 实例自带公网 endpoint | ✅ **推荐** |
| RDS MySQL 基础版 | ~¥50-100/月固定（1C 1G 起步），个人产品不划算 | ✅ 需开公网地址（另计流量/安全组） | ❌ 贵且重 |
| FC 内置 SQLite（/tmp） | 0 | ❌ /tmp 不持久（实例回收即丢），FC 无状态 | ❌ 不可用 |
| FC + OSS（JSON 文件当库） | 极低 | ❌ 要自己实现并发/一致性，状态机难做 | ❌ 工程量大 |

**为什么 Tablestore**：
- 按量模式免实例费、免运维，个人可开通（阿里云控制台搜索「表格存储」）；
- 天然 KV + 单行主键，订单按 `id` 点查/更新（`get`/`patch`）就是主键读写，语义与 D1 完全对齐；
- 支持原子自增（限流计数）、条件写（幂等/状态校验），足以复刻现有业务；
- daemon 从你电脑 30s 轮询走**公网 endpoint** 直连，无需中转服务。

**代价（MVP 接受）**：无 SQL，`getPendingPaid()`（按 status 过滤）只能全表 `getRange` 后内存过滤。
当前量级（每天几十单、全表几百行）30s 轮询毫无压力；**量大后再加「paid 队列表」**（daemon 改为范围读队列表，见 §7 TODO）。

### 2.1 Tablestore 表设计（与 D1 schema 对照）

实例建议与 FC 同地域 `cn-hongkong`（函数走内网更省；daemon 走公网）。

**表 1：`orders`**（主键 `id` STRING，一行一单；属性列 = D1 orders 全字段，`null` 不写、读回补 `null`）

| OTS 列 | 类型 | 对应 D1 | 说明 |
|---|---|---|---|
| `id`（PK） | STRING | id TEXT PK | 订单号 BM-XXX |
| `email` | STRING | email | 限流键之一 |
| `product_type` | STRING | product_type | adult / child |
| `book_title` | STRING | book_title | |
| `book_id` | STRING | book_id | 儿童白名单 id（成人无） |
| `duration_min` | INTEGER(Long) | duration_min | |
| `voice` | STRING | voice | |
| `age_band` | STRING | age_band | 3-6 / 7-12 |
| `parent_declared` | INTEGER(Long) | parent_declared | 1/0 |
| `amount_fen` | INTEGER(Long) | amount_fen | |
| `status` | STRING | status | 状态机字段 |
| `provider` | STRING | provider | 支付渠道 |
| `voucher` | STRING | voucher | 支付凭证 |
| `eta_min` | INTEGER(Long) | eta_min | |
| `created_day` | STRING | created_day | 限流键之一 |
| `created_at` | STRING | created_at | ISO8601 |
| `updated_at` | STRING | updated_at | |

**表 2：`rate_limits`**（主键 `key` STRING = `${email}#${created_day}`；属性列 `count` INTEGER）
- 解决「每邮箱每日 3 单」：`todayCount` 读该行 count；下单成功 `insert` 时对 count 做**原子 INCREMENT +1**（首单条件建行=1），避免并发下查-增竞态。
- 语义与 D1 版略有差异（D1 是 count 订单行）：计数只增不减，防滥用足够；退款/删单不影响计数，**更保守**（偏严可接受）。

---

## 3. 代码改动清单

| 文件 | 改动 | 说明 |
|---|---|---|
| `worker/src/worker.js` | 抽取 `export async function route(request, env)`；`export default` 委托 route（CF/测试行为不变）；`storeFor` 增加 `env.store` 平台注入优先级 | 业务逻辑 **0 改动**，FC 只复用 route |
| `fc/index.mjs` **新增** | FC 3.0 handler：event → 标准 Request → `route` → FC 响应对象；非 `/api/` 路径同源托管 `public/` | 平台适配层 + 静态托管 |
| `fc/store-ots.mjs` **新增** | Tablestore 存储适配层，实现 worker.js 5 方法接口（`todayCount/getPendingPaid/insert/get/patch`） | 缺 OTS 环境变量时返回 null → 自动回退内存 store |
| `fc/schema.ots.mjs` **新增** | 建表脚本（`node fc/schema.ots.mjs`，幂等），导出两表结构常量 | 008 初始化用 |
| `fc/s.yaml` **新增** | Serverless Devs fc3 部署配置（region=cn-hongkong、runtime=nodejs20、handler=index.handler、环境变量占位、自定义域名示例） | 008 `s deploy -y` 用 |
| `package.json` | 新增依赖 `tablestore@^5.6.5`；npm test 追加 `test/fc-handler.test.mjs` | SDK 已安装 |
| `test/fc-handler.test.mjs` **新增** | 9 条适配层测试：event 转换（query/base64 body）、静态托管、目录穿越拦截、handler 全链路、admin 401 | npm test 29/29 全绿 |
| `daemon/daemon.py` | **无需修改** | 生产只改启动参数：`--base-url https://<FC域名>` + `DAEMON_TOKEN` 环境变量 |
| `public/app.js` | **无需修改** | 生产同源相对路径 `API_BASE=''`；静态资源走同一 FC 域名 |
| `wrangler.toml` / `schema.sql` | 保留（CF 路径继续可本地开发） | 不删除 |

复用原则：**平台差异只发生在入口与存储两个接缝**。若未来要换回 CF 或换 AWS，只需换 `fc/` 这一层。

---

## 4. 部署步骤（008 控制台操作清单）

### A. 一次性准备（控制台）
1. **开通函数计算 FC 3.0**：选地域 **cn-hongkong**（中国香港，自定义域名**免 ICP 备案**；大陆地域要备案，见坑 2）。
2. **开通表格存储 Tablestore**（按量计费），同地域创建实例（如 `bookmadebook`）。记下实例名。
3. **建表**（二选一）：
   - 控制台 → 实例 → 数据管理 → 创建表（按 §2.1 两表结构）；
   - 或本机跑 `node fc/schema.ots.mjs`（需配好下面 AK 环境变量，且本机能访问 OTS 公网）。
4. **RAM 最小权限子账号 AK**（避免用主账号）：
   - 策略 1：`AliyunFCFullAccess`（部署 FC）——也可只在 s 配置里用；
   - 策略 2：`AliyunOTSFullAccess` 或更小 `ots:GetRow/PutRow/UpdateRow/GetRange/...`（函数运行时读表）；
   - 建议两个子账号或一个账号两把 AK 分开用途，最小暴露。
5. **域名**：准备一个域名（如 `book.xxx.com`），阿里云 DNS 解析到 FC 自定义域名后给出的 CNAME 目标。

### B. 本地配置 + 部署（008 在有 node 的机器）
```bash
cd /mnt/d/AI软件/GitHub/bookmadebook/webapp
# 1. 安装 Serverless Devs CLI（如未装）
#    npm i -g @serverless-devs/s   （或按官方文档装 s 命令）
# 2. 配置访问密钥（部署用 AK，勿提交仓库）
s config add --AccessKeyID <部署AK_ID> --AccessKeySecret <部署AK_SECRET> --alias default
# 3. 部署
s deploy -y
# 4.（可选）若 fc3 组件对 customDomains 支持不佳 → 改控制台手动建自定义域名：
#    控制台 → 函数 → 自定义域名 → 创建：域名 book.xxx.com、路径 / 路由到本函数、协议 HTTP
```

### C. 环境变量注入（控制台 → 函数 → 配置 → 环境变量）
| 变量 | 值 |
|---|---|
| `MOCK_PAY` | `0`（生产禁模拟支付；`1` 仅本地联调） |
| `DAEMON_TOKEN` | 强随机串（与本地 daemon 启动参数一致） |
| `TABLESTORE_ENDPOINT` | `https://<instance>.cn-hongkong.ots.aliyuncs.com`（公网；同地域函数可试内网 endpoint，见坑 4） |
| `TABLESTORE_INSTANCE` | 实例名 |
| `TABLESTORE_AK_ID` / `TABLESTORE_AK_SECRET` | 运行时读表 AK（也可换成 FC 服务角色，更安全） |

### D. 本地 daemon 切换（你电脑，无需改代码）
```bash
export BOOKMADE_BASE_URL="https://book.xxx.com"   # 生产 FC 自定义域名
export DAEMON_TOKEN="<与 C 一致>"
python3 daemon/daemon.py --once                     # 先单次验证
python3 daemon/daemon.py                            # 30s 常驻
```
联调期可先用默认域名（`xxx.cn-hongkong.fcapp.run`）：daemon 是 HTTP 客户端，不受「强制下载」头影响。

### E. 验收
- 浏览器开 `https://book.xxx.com` → H5 正常渲染（不是下载文件）；
- 下单 → 支付（MOCK_PAY=0 时接真实爱发电回调，未接前可用 008 联调开关）→ daemon 拉到单 → done；
- `node fc/schema.ots.mjs` 两次运行幂等；
- `npm test` 本地全绿（29/29）。

---

## 5. 坑清单（部署必读）

1. **fcapp.run 默认域名强制下载 ⚠️ 最重要**：FC 3.0 默认 HTTP 触发器域名（`{随机}.{region}.fcapp.run`）对响应强制加 `Content-Disposition: attachment` 且**不可修改**——浏览器打开会下载 HTML/JSON 而不是渲染。官方明确该域名仅供测试、禁止生产。**生产必须绑自定义域名**（绑定后解除强制下载）。API 侧（daemon）不受影响，联调可直接用默认域名。
2. **ICP 备案**：自定义域名绑**中国大陆地域** FC 需要 ICP 备案；**中国香港/海外地域免备案**。因此建议函数+OTS 都建在 `cn-hongkong`。008 部署时以阿里云控制台当前提示为准。
3. **/tmp 不持久**：FC 实例的 `/tmp` 随实例回收清空，禁止当数据库/产物存储。mp3 归档走 OSS（下一轮），本方案订单数据全在 Tablestore。
4. **Tablestore 公网流量计费**：daemon 从本地轮询必走公网 endpoint（量小：每次拉单+状态更新几 KB，月费可忽略）。函数与 OTS 同地域时，008 可尝试在函数侧配**内网/VPC endpoint** 进一步降费（若配置复杂，MVP 直接都走公网即可）。
5. **FC event 不是 Node req/res**：FC 3.0 是结构化 event（`requestContext.http.method/path`、`headers`、`queryParameters`、`body`+`isBase64Encoded`）。已由 `fc/index.mjs` 适配，勿在业务代码里假设 `req.url` 存在。
6. **响应头限制**：FC 会忽略 `content-length`/`date`/`server`/`content-disposition` 等头——不要在业务层依赖设置这些。CORS 头可正常返回（函数侧同源时基本用不到）。
7. **ESM 入口**：`fc/index.mjs` 用 `.mjs` + 具名导出 `handler`（s.yaml `handler: index.handler`）。若 008 部署时运行时不识别 `.mjs`，在 `fc/` 下加 `package.json`（`{"type":"module"}`）并把 `index.mjs` 改名 `index.js`，handler 配置不变。
8. **FC 超时/内存**：s.yaml 默认 256MB / 30s。订单 API 都是轻量 Tablestore 读写，足够；若 008 加「签名下载」等重逻辑再上调。
9. **限流窗口**：`todayCount` 先查后 `INCREMENT` 存在极小并发窗口（同一邮箱瞬间并发两单可能各放行一次）。防滥用场景可接受；若要严格，把「count < 3」条件写进 INCREMENT 的条件更新，失败即 429（下一轮可做）。
10. **免费额度以控制台为准**：FC 3.0 / Tablestore 的试用额度会随活动变化，方案里不做承诺；个人量级即便全按量付费也在几元/月量级。

---

## 6. 与 Cloudflare 路径的关系

- **本地开发**：`npm run dev`（wrangler + 本地 D1）与 `npm test` 完全不受影响（worker.js 默认导出未变）；
- **CF 生产**：原 `wrangler.toml`/D1/Pages 保留，未删除——如果阿里云验收不顺可随时回切；
- **迁移边界**：FC 版复用 `worker.js` 的 route；存储从 D1 换 Tablestore 只发生在 `fc/store-ots.mjs` 一个文件。

---

## 7. TODO（后续轮次）

- [ ] OSS 桶 + 签名下载：daemon 上传 mp3 → 订单 done 时 `download_url` 换成 OSS 签名链接（现仍为占位 `/api/download/:id`）；
- [ ] 爱发电真实回调验签替换 `MOCK_PAY` 分支（生产必须，FC 环境变量置 0）；
- [ ] 量级上来后：加 paid 队列表/二级索引，`getPendingPaid` 不再全表扫；
- [ ] 儿童次数卡（10 次 ¥39 / 30 次 ¥99）计费逻辑；
- [ ] 008 部署后把 `TABLESTORE_ENDPOINT` 内网化/换 FC 角色 AK，进一步降本提安全。
