# daemon OSS 交付配置示例（任务 4）
# 复制为同目录 .env 并填真实值（.env 已 gitignore，密钥不入库）：
#   cp daemon/.env.example daemon/.env
# shell export 优先于 .env（.env 只在对应变量未设置时生效）。

OSS_ENDPOINT=https://oss-cn-hongkong.aliyuncs.com   # 008 按实际 region 给
OSS_BUCKET=bookmadebook-audio
OSS_AK_ID=换成daemon上传专用AK
OSS_AK_SECRET=换成对应Secret
# OSS_RETRIES=3                # 上传重试次数（默认 3）
# 本地联调无真实 OSS 时取消下行注释（模拟上传，订单仍 done）：
# BOOKMADE_OSS_MOCK=1
